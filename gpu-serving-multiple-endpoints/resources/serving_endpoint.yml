resources:
  model_serving_endpoints:
    multi_embed_endpoint:
      name: ${var.endpoint_name}
      config:
        served_entities:
          - entity_name: ${var.catalog}.${var.schema}.${var.model_name}
            entity_version: "2"
            workload_size: Small
            workload_type: GPU_SMALL
            scale_to_zero_enabled: true
